This project is a real-time chat app created with Express and Socket.IO. It has a few basic functions for a chatting app.

## Setup and Run

1. Run `git clone https://github.com/1bukrek/real-time-chat-app.git` on terminal.
2. Open project directory and run `npm install` on terminal to install all node modules.
3. Run `cd server` and `node server` to start express server in project directory.
4. Go to `http://localhost:3000` in your browser to access server.
# real-time-chat-app
